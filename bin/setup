#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install
# Do any other automated setup that you need to do here

# FIXME : That would be a lot less hassle
# bundle exec rake bars:cache:reset
